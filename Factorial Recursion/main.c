#include <stdio.h>
#include <string.h>
int main() {
    char word[20];
    int n,left,right,i,t,len;

    printf("Enter a number.");
    scanf("%s",word);


    len=strlen(word);
    t=len;
    left=0;
    right=t-1;
    //for(i=right;i>=left;i--){
       // printf("%c",i);
    //}
    printf("%c",right);




}
